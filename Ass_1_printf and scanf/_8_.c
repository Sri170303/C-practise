// WAP program to caculate square of a given number. Number is entered by the user through the keyboard

#include <stdio.h>
int main(){
    int a;
    scanf("%d",&a);
    printf("Square of %d is %d",a,a*a);
    return 0;
}