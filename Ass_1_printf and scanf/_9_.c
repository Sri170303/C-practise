// WAP to calculate the area of a rectangle. Input appropriate data from the user
#include <stdio.h>
int main(){
    float l,b;
    printf("Enter length and breadth : ");
    scanf("%f %f",&l,&b);
    printf("Area = %f",l*b);
    return 0;
}